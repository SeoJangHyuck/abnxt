/**
 * `@abnxt/next/admin` — Next.js 16 App Router 네이티브 어드민.
 * `<AbnxtAdmin>`는 'use client' 컴포넌트(키폼 + 편집 UI). 편집 로직은 `@abnxt/core/admin` 공유.
 */
export { AbnxtAdmin, type AbnxtAdminProps } from './AbnxtAdmin';
